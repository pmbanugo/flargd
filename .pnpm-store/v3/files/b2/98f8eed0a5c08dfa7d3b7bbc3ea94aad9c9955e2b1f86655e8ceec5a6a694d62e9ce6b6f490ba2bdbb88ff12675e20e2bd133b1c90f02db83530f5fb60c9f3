import * as esbuild from "esbuild";
import type { RemixConfig } from "../../config";
import type { CompileOptions } from "../options";
export declare function vanillaExtractPlugin({ config, mode, outputCss, }: {
    config: RemixConfig;
    mode: CompileOptions["mode"];
    outputCss: boolean;
}): esbuild.Plugin;
